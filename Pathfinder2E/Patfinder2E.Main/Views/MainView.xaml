<UserControl x:Class="Pathfinder2E.Shell.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pathfinder2E.Shell.Views"
             mc:Ignorable="d" 
             xmlns:cmp="clr-namespace:Pathfinder2E.Shell.Components"
             xmlns:vm="clr-namespace:Pathfinder2E.Shell.ViewModels"
             d:DataContext="{d:DesignInstance vm:MainViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Grid x:Name="MainGrid" MinHeight="705">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*" />
                <ColumnDefinition Width="11*" />
            </Grid.ColumnDefinitions>
            <Border x:Name="LeftPanel" Grid.Column="0">
                <cmp:LeftPanel/>
            </Border>
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="79*"/>
                    <ColumnDefinition Width="31*"/>
                </Grid.ColumnDefinitions>
                <cmp:TabRightPanel Grid.ColumnSpan="2"/>
                <StackPanel Orientation="Horizontal" Margin="0,12,0,0" HorizontalAlignment="Center" VerticalAlignment="Top" Grid.Column="1" Height="24" Width="172">
                    <Button Margin="2"  Height="20" Content="Новый" Command="{Binding NewCommand}"/>
                    <Button Margin="2"   Height="20" Content="Сохранить" Command="{Binding SaveCommand}"/>
                    <Button Margin="2"  Height="20" Content="Загрузить" Command="{Binding LoadCommand}"/>
                </StackPanel>
            </Grid>

        </Grid>
    </StackPanel>
</UserControl>
