<UserControl x:Class="Pathfinder2E.Shell.Components.StatBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pathfinder2E.Shell.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel Width="85"  >
            <TextBox Height="77" Width="85" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" 
                     Text="{Binding SkillVal}" FontSize="24" FontWeight="Bold" />
            <TextBox Height="20" Width="85" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" 
                        Text="{Binding Value}" FontSize="14" FontWeight="Bold" />
            <Label Background="White" 
                   Content="{Binding Type}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
