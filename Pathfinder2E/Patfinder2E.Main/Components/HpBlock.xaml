<UserControl x:Class="Pathfinder2E.Shell.Components.HpBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pathfinder2E.Shell.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel>
            <DockPanel Background="White" LastChildFill="False">
                <TextBox Height="77" Width="75" HorizontalContentAlignment="Right" VerticalContentAlignment="Center" 
                     Text="{Binding Value}" FontSize="24" FontWeight="Bold" />
                <Label FontSize="24" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center">|</Label>
                <TextBox Height="77" Width="75" VerticalContentAlignment="Center" 
                     Text="{Binding MaxValue}" FontSize="24" FontWeight="Bold"/>
            </DockPanel>
            <Label Background="White" 
               Content="{Binding Type}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
